# Задача по миграции проекта с Next.js (React) на Nuxt 4 (Vue)

---

---

## **Контекст**

- В проекте предоставлены **две папки с frontend-ом**:

  - `adminka/` — старая реализация фронтенда на **Next.js (React)**. Содержит **всю вёрстку проекта**, включая публичную часть и админку.
  - `frontend/` — новая реализация на **Nuxt 4 (Vue)**. Содержит только заготовку публичной части.

- **Цель**:
  1. Перенести вёрстку из `adminka/` в `frontend/` (Nuxt 4).
  2. Разделить функционал: оставить админку в `adminka/`, а публичную часть — в `frontend/`.
  3. Задокументировать процесс миграции.

---

---

---

## **Задача 1: Перенос вёрстки из Next.js в Nuxt 4**

### **Цель**

Перенести **всю вёрстку** (стили, компоненты, шаблоны) из `adminka/` в `frontend/` с сохранением визуального сходства.

---

### **Список страниц для переноса**

Перенести вёрстку **всех** перечисленных страниц (включая стили, компоненты и расстановку элементов):

- `/` (home page)
- `/about/`
- `/author-variant/`
- `/cart/`
- `/feedback/`
- `/login/`
- `/new_test/`
- `/policy/`
- `/privacy/`
- `/profile/*`
- `/rules/`
- `/shop/*`
- `/terms/`

---

### **Требования к переносу**

1. **Стили и дизайн**:

   - Перенести **основные стили**:
     - Шрифты (`font-family`, размеры, веса).
     - Цвета (акцентный, основной, фоновый и др.).
     - Отступы, границы, тени и другие визуальные параметры.
   - Использовать **CSS/SCSS модули** или **Tailwind CSS** (если уже подключён в `frontend/`).
   - Если в `adminka/` используются **глобальные стили** (например, `globals.css`), перенести их в `frontend/assets/css/main.css` или аналогичный файл.

2. **Компоненты**:

   - Выделить **общие компоненты** (например, `Header`, `Footer`, `Container`, `Button`, `Card`) и перенести их в папку `frontend/components/`.
   - Разбить страницы на **подкомпоненты** (например, `HeroSection`, `TaskList`, `Sidebar`).
   - Использовать **композиционный API** (Vue 3) для логики компонентов.

3. **Шаблоны**:

   - Перенести **основной шаблон** (layout) из `adminka/` в `frontend/layouts/default.vue`.
   - Убедиться, что **header**, **footer** и **container** отображаются на всех страницах.

4. **Адаптивность**:

   - Проверить, что вёрстка корректно отображается на **мобильных устройствах** (если это было реализовано в `adminka/`).

5. **Иконки и статические файлы**:
   - Перенести **иконки**, **изображения** и другие статические файлы в `frontend/public/`.
   - Обновить пути к файлам в коде (например, `/images/logo.png` → `/logo.png`).

---

### **Примеры переноса**

#### **1. Перенос глобальных стилей**

- **Исходник** (`adminka/styles/globals.css`):
  ```css
  :root {
    --primary-color: #3498db;
    --accent-color: #e74c3c;
    --font-main: 'Roboto', sans-serif;
  }
  body {
    font-family: var(--font-main);
    margin: 0;
    padding: 0;
  }
  ```
- **Результат** (`frontend/assets/css/main.css`):
  ```css
  :root {
    --primary-color: #3498db;
    --accent-color: #e74c3c;
    --font-main: 'Roboto', sans-serif;
  }
  body {
    font-family: var(--font-main);
    margin: 0;
    padding: 0;
  }
  ```

#### **2. Перенос компонента `Header`**

- **Исходник** (`adminka/components/Header.jsx`):
  ```jsx
  export default function Header() {
    return (
      <header className="header">
        <div className="container">
          <a href="/" className="logo">
            Logo
          </a>
          <nav className="nav">
            <a href="/about">About</a>
            <a href="/login">Login</a>
          </nav>
        </div>
      </header>
    );
  }
  ```
- **Результат** (`frontend/components/Header.vue`):

  ```vue
  <template>
    <header class="header">
      <div class="container">
        <NuxtLink to="/" class="logo">Logo</NuxtLink>
        <nav class="nav">
          <NuxtLink to="/about">About</NuxtLink>
          <NuxtLink to="/login">Login</NuxtLink>
        </nav>
      </div>
    </header>
  </template>

  <style scoped>
  .header {
    background: var(--primary-color);
    padding: 1rem 0;
  }
  .logo {
    color: white;
    font-weight: bold;
  }
  </style>
  ```

#### **3. Перенос страницы `/new_test/`**

- **Исходник** (`adminka/pages/new_test.jsx`):
  ```jsx
  export default function NewTestPage() {
    return (
      <div className="container">
        <h1>Новый тест</h1>
        <div className="test-content">{/* Контент теста */}</div>
      </div>
    );
  }
  ```
- **Результат** (`frontend/pages/new_test.vue`):

  ```vue
  <template>
    <div class="container">
      <h1>Новый тест</h1>
      <div class="test-content">
        <!-- Контент теста -->
      </div>
    </div>
  </template>

  <script setup>
  // Логика компонента (если есть)
  </script>

  <style scoped>
  .test-content {
    margin-top: 2rem;
  }
  </style>
  ```

---

---

---

## **Задача 2: Разделение функционала между проектами**

### **Цель**

- Оставить в `adminka/` **только админский функционал** (роут `/admin`).
- Удалить из `frontend/` **весь функционал, связанный с `/admin`**.
- Перенести админский функционал из `adminka/` в корень приложения `frontend/` (если он там отсутствует).

---

### **Шаги**

#### **1. Очистка `adminka/`**

- Удалить **все файлы и папки**, не связанные с роутом `/admin`:
  - Удалить страницы: `/`, `/about/`, `/login/` и др. (оставить только `/admin`).
  - Удалить компоненты, которые используются **только в публичной части** (например, `Header`, `Footer` для публичных страниц).
  - Оставить **только**:
    - Страницы и компоненты, связанные с `/admin`.
    - Логику авторизации для админки.
    - Стили, специфичные для админки.

#### **2. Очистка `frontend/`**

- Удалить **весь функционал, связанный с `/admin`**:
  - Удалить страницы `/admin` и все подстраницы (например, `/admin/dashboard`).
  - Удалить компоненты, которые используются **только в админке**.
  - Удалить роуты, связанные с админкой (в `nuxt.config.ts` или `app.vue`).

#### **3. Перенос админского функционала в `frontend/`**

- Если в `adminka/` есть **уникальный функционал админки**, который отсутствует в `frontend/`:
  - Перенести его в `frontend/` (например, в папку `frontend/pages/admin/`).
  - Обновить роуты в `frontend/` для поддержки `/admin`.

---

---

---

## **Задача 3: Документирование миграции**

### **Цель**

Создать файл **`10_migration_report.md`** в корне архива с описанием:

1. Что было перенесено.
2. Какие проблемы возникли и как они были решены.
3. Какие компоненты/страницы требуют доработки.

---

### **Структура файла `10_migration_report.md`**

```markdown
# Отчёт о миграции с Next.js на Nuxt 4

---

## **1. Общий обзор**

- **Дата**: [ДД.ММ.ГГГГ]
- **Цель**: Перенос вёрстки и разделение функционала между `adminka/` и `frontend/`.

---

## **2. Перенесённые элементы**

### **2.1. Глобальные стили**

| Элемент         | Статус | Примечания                    |
| --------------- | ------ | ----------------------------- |
| Шрифты          | ✅/❌  | Перенесены в `main.css`       |
| Цвета           | ✅/❌  | Перенесены в CSS-переменные   |
| Отступы/границы | ✅/❌  | Перенесены в глобальные стили |

### **2.2. Компоненты**

| Компонент | Статус | Примечания                          |
| --------- | ------ | ----------------------------------- |
| Header    | ✅/❌  | Перенесён в `components/Header.vue` |
| Footer    | ✅/❌  | Перенесён в `components/Footer.vue` |
| Container | ✅/❌  | Перенесён в `layouts/default.vue`   |
| Button    | ✅/❌  | Требует доработки стилей            |

### **2.3. Страницы**

| Страница     | Статус | Примечания                    |
| ------------ | ------ | ----------------------------- |
| `/`          | ✅/❌  | Перенесена, проверена вёрстка |
| `/about/`    | ✅/❌  | Перенесена                    |
| `/new_test/` | ✅/❌  | Требует доработки логики      |
| `/admin/*`   | ❌     | Удалена из `frontend/`        |

---

## **3. Проблемы и решения**

| Проблема                     | Решение              | Статус |
| ---------------------------- | -------------------- | ------ |
| Несовместимость стилей       | Переписаны на SCSS   | ✅     |
| Отсутствие компонента в Nuxt | Создан аналог на Vue | ✅     |
| Расхождения в вёрстке        | Ручная правка CSS    | ⚠️     |

---

## **4. Оставшиеся задачи**

- [ ] Доработать стили для страницы `/new_test/`.
- [ ] Проверить адаптивность на мобильных устройствах.
- [ ] Оптимизировать загрузку шрифтов.

---

## **5. Выводы**

- **Что сделано**: [Описание]
- **Что требует доработки**: [Описание]
- **Рекомендации**: [Описание]
```

---

---

---

## **Требования к результату**

1. **Архив с результатом**:

   - Папка `adminka/` — только админский функционал.
   - Папка `frontend/` — только публичная часть + перенесённая вёрстка.
   - Файл `10_migration_report.md` — отчёт о миграции.

2. **Проверка**:

   - Убедиться, что **все страницы** из списка открываются в `frontend/` и отображаются корректно.
   - Убедиться, что в `adminka/` остался **только `/admin`**.
   - Убедиться, что в `frontend/` **нет следов админки**.

3. **Дополнительно**:
   - Если в процессе переноса возникли **вопросы по вёрстке** (например, неясно, как разбить компонент), зафиксировать их в `10_migration_report.md` и предложить варианты решений.

---

---

## **Примечания для агента**

- **Приоритеты**:

  1. Перенос вёрстки и стилей.
  2. Разделение функционала между проектами.
  3. Документирование.

- **Что не делать**:

  - Не тратить время на **логику работы** (например, генерацию вариантов, авторизацию).
  - Не переносить **бэкенд-код** (только фронтенд).
  - Не оптимизировать производительность (только вёрстка и структура).

- **Что делать, если что-то непонятно**:
  - Зафиксировать вопрос в `10_migration_report.md`.
  - Предложить свой вариант решения.

---

---

## **Критерий завершённости задачи**

✅ **Готово**, если:

1. В `frontend/` перенесена вёрстка **всех указанных страниц**.
2. В `adminka/` остался **только `/admin`**.
3. В `frontend/` **нет админского функционала**.
4. Есть файл `10_migration_report.md` с отчётом.
